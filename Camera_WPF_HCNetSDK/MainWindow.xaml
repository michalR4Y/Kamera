<Window x:Class="Camera_WPF_HCNetSDK.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Camera_WPF_HCNetSDK"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="150*" />
            <RowDefinition Height="300*" />
            <RowDefinition Height="150*" />
        </Grid.RowDefinitions>

        <!-- Wiersz 1 z własnym podziałem -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
        </Grid>

        <!-- Wiersz 2 z innym podziałem -->
        <Grid Margin="0,196,0,10" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Image x:Name="LeftCamera" Margin="0,-191,0,0"  Width="Auto" Stretch="Uniform" />
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Image Margin="0,0,0,0" Grid.Column="0" Source="Icons/KompasCopy.png" Stretch="Fill" RenderTransformOrigin="0.5,0.5">
                <Image.RenderTransform>
                    <RotateTransform Angle="0" />
                </Image.RenderTransform>
            </Image>
            <Image Margin="0,0,0,0" Grid.Column="1" Source="Icons/SztucznyHoryzont.png" Stretch="Fill" RenderTransformOrigin="0.5,0.5">
                <Image.RenderTransform>
                    <RotateTransform Angle="45" />
                </Image.RenderTransform>
            </Image>

            <Button x:Name="ButtonD3" Grid.Column="2" Content="D_3" />
            <Button x:Name="ButtonD4" Grid.Column="3" Content="D_4" />
            <Button x:Name="ButtonD5" Grid.Column="4" Content="D_5" FontSize="30" />
            <Button x:Name="ButtonStart" Grid.Column="5" FontSize="20px" Content="Start" Click="ButtonStart_Click" />

        </Grid>
    </Grid>
</Window>
